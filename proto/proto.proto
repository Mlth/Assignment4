syntax = "proto3";

package ring;
option go_package = "https://github.com/Mlth/Assignment4;ring";


service Ring {
    rpc send (Request) returns (emptyMessage) {}
}

message Request {
    int32 id = 1;
    string type = 2;
}

message emptyMessage {
}